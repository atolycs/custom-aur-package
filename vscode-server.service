[Unit]
Description=code-server
After=network.target

[Service]
Type=exec
ExecStart=/usr/bin/code-server --without-connection-token \
          --host 0.0.0.0 \
	  --accept-server-license-terms \
          --start-server
Restart=always

[Install]
WantedBy=default.target
